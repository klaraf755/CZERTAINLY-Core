{
  "type": "object",
  "required": ["sequence"],
  "properties": {
    "sequence": {
      "type": "array",
      "maxItems": 2,
      "prefixItems": [
        {
          "title": "cA",
          "type": "object",
          "required": ["boolean"],
          "properties": { "boolean": { "const": true } }
        },
        {
          "title": "pathLenConstraint",
          "type": "object",
          "required": ["integer"],
          "properties": { "integer": { "type": "integer", "minimum": 0 } }
        }
      ]
    }
  }
}
