{
  "type": "object",
  "required": ["sequence"],
  "properties": {
    "sequence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["sequence"],
        "properties": {
          "sequence": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "prefixItems": [
              { "title": "type", "type": "object", "required": ["oid"] },
              {
                "title": "values",
                "type": "object",
                "required": ["set"],
                "properties": { "set": { "type": "array", "minItems": 1 } }
              }
            ]
          }
        }
      }
    }
  }
}
